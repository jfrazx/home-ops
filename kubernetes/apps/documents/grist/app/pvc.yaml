---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-apps-grist-pvc
  namespace: documents
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: truenas-nfs
