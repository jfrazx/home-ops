---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-apps-paperless-pvc
  namespace: documents
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  storageClassName: nfs-csi
  volumeName: nfs-apps-paperless-pv
