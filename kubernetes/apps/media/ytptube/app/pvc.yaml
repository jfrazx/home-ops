---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-youtube-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 6Ti
  storageClassName: nfs-csi
  volumeName: nfs-youtube-pv
