---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-backups
  namespace: household
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: truenas-nfs-backups
  resources:
    requests:
      storage: 50Gi
